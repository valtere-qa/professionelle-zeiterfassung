{
  "name": "professionelle-zeiterfassung",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply zeiterfassung-cloud-prod --remote",
    "test": "node --test tests/routing.test.cjs"
  },
  "devDependencies": { "wrangler": "^4.0.0", "jsdom": "26.1.0" }
}
